{% extends 'base.html.twig' %}

{% block title %}Periodo{% endblock %}

{% block body %}
    <h1>Periodo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ periodo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ periodo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_periodo_index') }}">back to list</a>

    <a href="{{ path('app_periodo_edit', {'id': periodo.id}) }}">edit</a>

    {{ include('periodo/_delete_form.html.twig') }}
{% endblock %}
